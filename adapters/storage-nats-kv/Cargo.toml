[package]
name = "storage-nats-kv"
version = "0.1.0"
edition = "2021"
description = "NATS JetStream KV storage adapter for grant nonces and session states"

[dependencies]
async-nats = "0.38"
serde_json = "1.0"
anyhow = "1.0"
