[package]
name = "trust-audit"
version = "0.1.0"
edition = "2021"
description = "Hash-chained audit events and audit sinks"

[dependencies]
trust-model = { path = "../trust-model" }
trust-canonical = { path = "../trust-canonical" }
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
