[package]
name = "trust-model"
version = "0.1.0"
edition = "2021"
description = "Pure, zero-dependency model structs for Open Execution Authorization Protocol"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
schemars = "0.8"
chrono = { version = "0.4", features = ["serde"] }
