[package]
name = "trust-reference-executor"
version = "0.1.0"
edition = "2021"
description = "Reference in-memory Executor implementation for standalone zero-dependency testing"

[dependencies]
trust-model = { path = "../trust-model" }
trust-canonical = { path = "../trust-canonical" }
trust-executor-sdk = { path = "../trust-executor-sdk" }
trust-egress = { path = "../trust-egress" }
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
