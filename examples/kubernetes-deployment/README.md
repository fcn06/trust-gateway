# Kubernetes Deployment Example

This directory contains production-ready Kubernetes manifests for deploying the Lianxi Trust Gateway service inside a Kubernetes cluster.

## Deployment

```bash
kubectl apply -f configmap.yaml
kubectl apply -f service.yaml
kubectl apply -f deployment.yaml
```
