apiVersion: v1
kind: Service
metadata:
  name: trust-gateway
  namespace: lianxi-system
spec:
  type: ClusterIP
  ports:
    - port: 3060
      targetPort: 3060
      protocol: TCP
      name: http
  selector:
    app: trust-gateway
