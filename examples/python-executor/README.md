# Python Executor Example

This example demonstrates how a third-party Python service or executor validates `ExecutionGrant` JWT tokens and verifies input hash binding before running an authorized action.

## Usage

```bash
python3 main.py
```
