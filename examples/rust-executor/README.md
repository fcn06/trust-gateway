# Rust Executor Example

This example demonstrates how a third-party Rust service uses `trust_verifier` to validate `ExecutionGrant` claims and input hash bindings before tool execution.

## Running

```bash
cargo run
```
