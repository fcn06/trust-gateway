[package]
name = "policy_sdk"
version = "0.1.0"
edition = "2021"
description = "4-layer hierarchical policy engine for zero-trust authorization"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
toml = "0.8"
trust_verifier = { path = "../verifier" }
trust-model = { path = "../crates/trust-model" }

