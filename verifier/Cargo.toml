[package]
name = "trust_verifier"
version = "0.1.0"
edition = "2021"
description = "Lightweight, zero-trust execution grant verifier library"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
schemars = "0.8"
sha2 = "0.10"
jwt-simple = { version = "0.12", default-features = false, features = ["pure-rust"] }


